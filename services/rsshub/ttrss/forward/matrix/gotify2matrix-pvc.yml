apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: ttrss
  name: gotify2matrix
  namespace: rsshub
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: "rsshub"
  resources:
    requests:
      storage: 1Gi
